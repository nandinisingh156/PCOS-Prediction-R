# ============================================================
# requirements.txt — PCOS Prediction R Project
# ============================================================
# R version required: >= 4.0.0
# Tested on        : R 4.5.3 (as shown in VS Code status bar)
# ============================================================

# ── Core Data Manipulation ───────────────────────────────────
tidyverse       # >= 2.0.0   | ggplot2, dplyr, tidyr, readr, tibble, scales
dplyr           # >= 1.1.0   | data wrangling (included in tidyverse)
tidyr           # >= 1.3.0   | pivot_longer, pivot_wider (included in tidyverse)
readr           # >= 2.1.0   | fast CSV reading (included in tidyverse)
tibble          # >= 3.2.0   | rownames_to_column (included in tidyverse)
scales          # >= 1.3.0   | percent labels on plots (included in tidyverse)

# ── Machine Learning ─────────────────────────────────────────
caret           # >= 6.0.94  | model training, cross-validation, confusionMatrix
randomForest    # >= 4.7.1   | Random Forest algorithm
xgboost         # >= 1.7.0   | XGBoost algorithm

# ── Model Evaluation ─────────────────────────────────────────
pROC            # >= 1.18.0  | ROC curves and AUC scoring

# ── Visualisation ────────────────────────────────────────────
ggplot2         # >= 3.4.0   | all plots (included in tidyverse)
corrplot        # >= 0.92    | correlation matrix heatmap
pdp             # >= 0.8.1   | partial dependence plots

# ============================================================
# HOW TO INSTALL ALL PACKAGES AT ONCE
# Copy and paste this block into your R console:
# ============================================================
#
# install.packages(c(
#   "tidyverse",
#   "caret",
#   "randomForest",
#   "xgboost",
#   "pROC",
#   "corrplot",
#   "pdp"
# ), dependencies = TRUE)
#
# ============================================================
# VERIFY INSTALLATION — run this in R console:
# ============================================================
#
# pkgs <- c("tidyverse","caret","randomForest","xgboost","pROC","corrplot","pdp")
# installed <- pkgs %in% installed.packages()[,"Package"]
# cat("Missing:", pkgs[!installed], "\n")
#
# ============================================================
# SYSTEM REQUIREMENTS
# ============================================================
# OS      : Windows 10/11, macOS 12+, Ubuntu 20.04+
# RAM     : Minimum 4 GB (8 GB recommended for XGBoost)
# R       : https://cran.r-project.org/
# RTools  : https://cran.r-project.org/bin/windows/Rtools/
#           (required on Windows for compiling packages)
# VS Code : https://code.visualstudio.com/
# R Extension for VS Code: REditorSupport.r